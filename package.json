{
  "name": "claude-quota",
  "version": "1.0.0",
  "description": "Query Claude Code subscription quota using independently authorized OAuth credentials.",
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "claude-quota": "./claude-quota.ts"
  },
  "scripts": {
    "start": "tsx claude-quota.ts",
    "json": "tsx claude-quota.ts --json",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "tsx": "^4.19.2"
  },
  "allowScripts": {
    "esbuild@0.28.2": true,
    "fsevents": false
  }
}
